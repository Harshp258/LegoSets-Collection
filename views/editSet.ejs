<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #FFEB3B;
            margin: 0;
            padding: 0;
            background-image: url('https://t3.ftcdn.net/jpg/03/21/48/06/360_F_321480639_mVJIrZLLOH0pEd00Bo2vw1LA3k8IpnjR.jpg');
            background-repeat: repeat;
        }

        .navbar {
            background-color: #F44336;
            color: #fff;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            border-radius: 4px;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.2);
        }

        .navbar a {
            color: #fff;
            text-decoration: none;
            margin: 0 10px;
            padding: 8px 16px;
            border-radius: 4px;
            background: rgba(255, 255, 255, 0.1);
        }

        .navbar a:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .container {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            background-color: #fff; /* Updated background color */
        }

        .hero {
            background-color: #2196F3;
            color: #fff;
            padding: 20px;
            text-align: center;
            border-radius: 8px;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.2);
        }

        form {
            margin-top: 20px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #333;
        }

        input,
        select {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            display: inline-block;
            border: 1px solid #ccc;
            box-sizing: border-box;
            border-radius: 4px;
        }

        button {
            background-color: #4CAF50;
            color: #fff;
            border: none;
            padding: 12px 20px;
            border-radius: 8px;
            text-decoration: none;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.2);
            cursor: pointer;
            display: inline-block;
        }

        button:hover {
            background-color: #388E3C;
        }
    </style>
    <title>Edit Set - Lego Sets Collection</title>
</head>

<body>
    <div class="navbar">
        <a href="/" style="font-size: 24px;">Lego Fantasia</a>
        <div style="display: flex; align-items: center;"></div>
    </div>
    <div class="container">
        <div class="hero">
            <h1>Edit Set</h1>
            <p>Edit the details below and click "Update Set" to save changes.</p>
        </div>

        <form action="/lego/editSet" method="post">
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" value="<%= set.name %>" required>

            <label for="year">Year:</label>
            <input type="number" id="year" name="year" value="<%= set.year %>" required>

            <label for="num_parts">Number of Parts:</label>
            <input type="number" id="num_parts" name="num_parts" value="<%= set.num_parts %>" required>

            <label for="img_url">Image URL:</label>
            <input type="url" id="img_url" name="img_url" value="<%= set.img_url %>" required>

            <label for="theme_id">Theme:</label>
            <select id="theme_id" name="theme_id" required>
                <% themes.forEach(theme => { %>
                <option <%= (set.theme_id == theme.id) ? "selected" : "" %> value="<%= theme.id %>">
                    <%= theme.name %>
                </option>
                <% }) %>
            </select>

            <label for="set_num">Set Number:</label>
            <input type="text" id="set_num" name="set_num" value="<%= set.set_num %>" readonly required>

            <button type="submit" class="btn btn-success">Update Set</button>
            <a href="/lego/deleteSet/<%= set.set_num %>" class="btn btn-error" style="text-decoration: none; color: #fff; background-color: #e57373; padding: 12px 20px; border-radius: 8px; box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.2); cursor: pointer; display: inline-block;">
                Delete Set
            </a>
            
        </form>
    </div>
</body>

</html>
